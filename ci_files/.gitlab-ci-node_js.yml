image: node

pages:
  stage: deploy
  cache:
    paths:
      - node_modules/
  script:
    - mkdir public
    - cd node_js/
    - npm install
    - npm run build
    - mv dist/ public/
    - mv plugin/ public/
    - mv css/ public/
    - mv img/ public/
    - mv videos/ public/
    - mv iframes/ ../public/
    - mv index.html public/index.html
  artifacts:
    paths:
      - public
  only:
    - master
